name: Diagnose Docusaurus Build

on:
  push:
    branches:
      - main  # Ajuste para a branch principal

jobs:
  diagnose:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: docs/package-lock.json

      - name: Instalar dependências
        run: npm ci
        working-directory: docs

      - name: Executar build e detectar links quebrados
        run: |
          echo "🔍 Rodando diagnóstico de links quebrados..."
          npm run build -- --no-minify || echo "❌ Build falhou devido a links quebrados!"
        working-directory: docs
        continue-on-error: true  # Continua o workflow mesmo se encontrar links quebrados

      - name: Gerar relatório de links quebrados
        if: always()
        run: |
          echo "📊 Relatório de links quebrados:"
          grep -E "Broken link on source page path" docs/build.log || echo "✅ Nenhum link quebrado encontrado!"
        working-directory: docs
        continue-on-error: true

      - name: Forçar falha se houver links quebrados críticos
        if: always()
        run: |
          if grep -q "Broken link on source page path" docs/build.log; then
            echo "❌ Links quebrados encontrados! Corrija-os antes do deploy."
            exit 1
          else
            echo "✅ Nenhum erro crítico encontrado."
          fi
        working-directory: docs
