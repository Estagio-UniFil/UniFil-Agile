name: Deploy Docusaurus to GitHub Pages

on:
  push:
    branches:
      - main  # Altere para a branch principal do seu projeto, se necessário

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'  # Ajuste conforme necessário

      - name: Instalar dependências
        working-directory: documentation
        run: npm install

      - name: Build do Docusaurus
        working-directory: documentation
        run: npm run build

      - name: Configurar GitHub Pages
        uses: actions/configure-pages@v4

      - name: Fazer upload dos arquivos estáticos
        uses: actions/upload-pages-artifact@v2  # Atualizado para v2
        with:
          path: documentation/build  # O diretório correto onde o Docusaurus gera os arquivos

      - name: Publicar no GitHub Pages
        uses: actions/deploy-pages@v2  # Atualizado para v2
